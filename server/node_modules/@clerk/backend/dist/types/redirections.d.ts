type RedirectAdapter = (url: string) => any;
type SingUpParams = {
    url?: string;
    returnBackUrl?: string;
};
type SingInParams = {
    url?: string;
    returnBackUrl?: string;
};
type RedirectParams = {
    redirectAdapter: RedirectAdapter;
    signInUrl?: string;
    signUpUrl?: string;
    publishableKey?: string;
    frontendApi?: string;
};
export declare function redirect({ redirectAdapter, signUpUrl, signInUrl, frontendApi, publishableKey }: RedirectParams): {
    redirectToSignUp: ({ returnBackUrl }?: SingUpParams) => any;
    redirectToSignIn: ({ returnBackUrl }?: SingInParams) => any;
};
export {};
//# sourceMappingURL=redirections.d.ts.map